# Remote host example: forward hook payloads back to your local c-notify receiver.
notify = ["python3", "/ABSOLUTE/PATH/TO/c-notify/c-notify.py", "relay", "--tool", "codex", "--endpoint", "http://127.0.0.1:38765"]

[features]
codex_hooks = true
